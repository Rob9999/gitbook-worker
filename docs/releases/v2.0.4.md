---
version: 2.0.4.post1
date: 2025-12-31
status: released
history:
  - "init: 2025-12-31"
---

# GitBook Worker v2.0.4 (Hotfix) Release Notes

**Release Date**: December 31, 2025  
**Tag (GitHub)**: release-v.2.0.4-hotfix  
**Packaging Version**: 2.0.4.post1 (PEP 440)

## 🎯 Summary
- Fix converter launch to always use `gitbook_worker.tools.converter.convert_assets`, avoiding collisions with user-local `tools` packages on the same machine.
- Add customer installation/run guide with clean venv steps, uninstall of stale `tools`, and verification commands.
- README updated to surface the new release and customer guidance.

## 🔧 Changes
- Orchestrator converter step now calls the fully qualified module path to bypass neighbouring `tools` modules.
- New doc `docs/customer-installation.md` explaining pip install, venv activation, uninstall of old modules, and orchestrator usage.
- README refreshed for v2.0.4 and linked to the new release notes.

## ✅ Testing
- `python -m pytest gitbook_worker/tests -m "not slow" -q` (374 passed, 10 skipped, 10 deselected; 4x pathlib.Path contextmanager deprecation warnings).
- Orchestrator local (converter step): `python -m gitbook_worker.tools.workflow_orchestrator run --root . --profile local --lang de --step converter`.

## 📌 Notes
- Use tag `release-v.2.0.4-hotfix`; keep packaging at `2.0.4.post1` for PEP 440 compliance.
- When running on machines with multiple projects containing `tools/`, ensure the project venv is active; see `docs/customer-installation.md`.
- Docker-based smoke run not executed in this workspace; recommended command: `python -m gitbook_worker.tools.docker.run_docker orchestrator --profile local --use-dynamic --lang de --root <repo>`.
