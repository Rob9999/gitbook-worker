---
version: 2.0.3.post1
date: 2025-12-31
status: released
history:
  - "init: 2025-12-31"
---

# GitBook Worker v2.0.3.post1 (Hotfix) Release Notes

**Release Date**: December 31, 2025  
**Tag (GitHub)**: release-v.2.0.3-hotfix  
**Packaging Version**: 2.0.3.post1 (PEP 440)

## 🎯 Summary
- Hotfix to keep emoji visual-check artifacts inside the pytest temp tree instead of `de/publish`.
- Restored PEP 440 compliant version string for packaging (2.0.3.post1).
- Documented hotfix tag format in the contributor guide (AGENTS.md).

## 🔧 Changes
- `test_emoji_color_visual_check` now writes extracted PNGs to `gitbook_worker/tests/tmp/artifacts/test_emoji_color_visual_check/`, preventing publish directory pollution.
- Package version aligned across `setup.cfg` and `gitbook_worker/__init__.py` to 2.0.3.post1 for build/install compatibility.
- Added release tagging note (including hotfix labels) to `AGENTS.md` under "How to release".

## ✅ Testing
- `python -m pytest gitbook_worker/tests -m "not slow" -q` (pass: 374, skipped: 10, deselected: 10; warnings: pathlib.Path contextmanager deprecation).

## 📌 Notes
- Manual emoji colour verification test remains marked `@manual`; open the generated PNGs under `tests/tmp/artifacts` when running that check.
- Remove any previously generated `de/publish/emoji-test-page-*.png` artifacts if present; they are no longer used by tests.
