---
version: 1.0.0
date: 2026-02-08
history:
  - "1.0.0: 2026-02-08 — Initial release notes for v2.2.1 „Einheitlich""
---

# v2.2.1 „Einheitlich" — Release Notes

**Datum / Date:** 8. Februar 2026
**Tag:** `v2.2.1`
**Codename:** „Einheitlich" (Consistent)

---

## 🇩🇪 Deutsch

### Zusammenfassung

Patch-Release zur Herstellung vollständiger Versionskonsistenz im gesamten
Repository. Alle `publish.yml`-Dateien referenzieren nun einheitlich die
aktuelle Schema-Version `0.1.1` — in Dokumentationsbeispielen, Test-Szenarien
und Edge-Case-Konfigurationen.

### Änderungen

#### 🔧 publish.yml-Versionskonsistenz (9 Dateien)

Alle `publish.yml`-Dateien von `version: 0.1.0` auf `version: 0.1.1` aktualisiert:

| Datei | Kontext |
|-------|---------|
| `README.md` (DE-Beispiel, Zeile ~188) | Nutzer-Dokumentation |
| `README.md` (EN-Beispiel, Zeile ~573) | Nutzer-Dokumentation |
| `gitbook_worker/tests/data/scenario-1-single-gitbook/publish.yml` | Test-Szenario |
| `gitbook_worker/tests/data/scenario-2-multi-gitbook/publish.yml` | Test-Szenario |
| `gitbook_worker/tests/data/scenario-3-single-file/publish.yml` | Test-Szenario |
| `gitbook_worker/tests/data/scenario-4-folder-without-gitbook/publish.yml` | Test-Szenario |
| `gitbook_worker/tests/data/workspace/publish.yml` | Test-Workspace |
| `de-edge-cases/publish.yml` | Edge-Case-Konfiguration |
| `en-edge-cases/publish.yml` | Edge-Case-Konfiguration |

**Bereits korrekt (unverändert):**

| Datei | Version |
|-------|---------|
| `de/publish.yml` | `0.1.1` ✅ |
| `en/publish.yml` | `0.1.1` ✅ |
| `customer-de/publish.yml` | `0.1.1` ✅ |

### Motivation

Die AGENTS.md Config-Completeness-Policy (§25–30) verlangt konsistente
Versionierung aller Konfigurationsdateien. Bei v2.2.0 wurde die
`publish.yml`-Schema-Version auf `0.1.1` angehoben, aber nicht alle
Referenzen im Repository wurden aktualisiert. Dieses Release schließt
diese Lücke.

### Tests

- Bestehende Testbasis (452+ Tests) unverändert — Versionsfeld ist nicht
  funktionsrelevant für den Publisher, daher keine Regression erwartet.

---

## 🇬🇧 English

### Summary

Patch release ensuring full version consistency across the repository.
All `publish.yml` files now uniformly reference the current schema version
`0.1.1` — in documentation examples, test scenarios, and edge-case
configurations.

### Changes

#### 🔧 publish.yml version consistency (9 files)

All `publish.yml` files updated from `version: 0.1.0` to `version: 0.1.1`:

| File | Context |
|------|---------|
| `README.md` (DE example, line ~188) | User documentation |
| `README.md` (EN example, line ~573) | User documentation |
| `gitbook_worker/tests/data/scenario-1-single-gitbook/publish.yml` | Test scenario |
| `gitbook_worker/tests/data/scenario-2-multi-gitbook/publish.yml` | Test scenario |
| `gitbook_worker/tests/data/scenario-3-single-file/publish.yml` | Test scenario |
| `gitbook_worker/tests/data/scenario-4-folder-without-gitbook/publish.yml` | Test scenario |
| `gitbook_worker/tests/data/workspace/publish.yml` | Test workspace |
| `de-edge-cases/publish.yml` | Edge-case configuration |
| `en-edge-cases/publish.yml` | Edge-case configuration |

**Already correct (unchanged):**

| File | Version |
|------|---------|
| `de/publish.yml` | `0.1.1` ✅ |
| `en/publish.yml` | `0.1.1` ✅ |
| `customer-de/publish.yml` | `0.1.1` ✅ |

### Motivation

The AGENTS.md Config Completeness Policy (§25–30) requires consistent
versioning across all configuration files. In v2.2.0 the `publish.yml`
schema version was raised to `0.1.1`, but not all references in the
repository were updated. This release closes that gap.

### Tests

- Existing test suite (452+ tests) unchanged — the version field is not
  functionally relevant for the publisher, so no regression expected.
